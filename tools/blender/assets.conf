# Asset registry for the Blender -> Godot unit pipeline.
#
# One line per asset, pipe-separated. reingest.sh reads this so re-exporting a
# changed sculpt is a single command instead of remembering flags. Add a line
# here when you add a unit; nothing else needs to know about it.
#
# name | source .blend (absolute) | output .glb (repo-relative) | height | face budget
#
# height      final height in world units (cells are 2.0)
# face budget decimation target; the result lands near 2x this in TRIANGLES

tree_grunt|C:/Users/kenne/OneDrive/Documents/CONQUEST/tree_grunt.blend|game/characters/models/forest/tree_grunt.glb|1.8|5000
